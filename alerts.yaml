
##  Note: leading spaces in each line are critical
##  to yaml interpretation - do not delete them


###  ALERTS  ###

# CURRENTLY NOT USED - COMMENTED OUT IN CONFIGURATION.YAML
# too limiting:
# - cannot terminate after X many notiiications
# - cannot use script (say_phrase) instead of notification

garage_door:
  name: Large garage door is open
  entity_id: cover.large_garage_door
  state: 'open'
  repeat: 30
  can_acknowledge: True
  skip_first: True
  notifiers:
    - garage_open


